/** Window foundation. Mode chrome and onboarding land in PR-04 and PR-06. */
export function App() {
  return (
    <main className="harbor-shell">
      <h1>Harbor</h1>
      <p>an open desktop host for coding agents</p>
      <span>Free · local</span>
    </main>
  );
}
