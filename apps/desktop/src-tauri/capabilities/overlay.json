{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Hidden dictation overlay: receive host events only; no filesystem, network, clipboard, or process access.",
  "local": true,
  "windows": ["overlay"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
