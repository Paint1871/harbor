[package]
name = "harbor-git"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Workspace git diffs for Harbor"

[dependencies]
harbor-paths = { path = "../harbor-paths" }

[dev-dependencies]
tempfile = "=3.27.0"
